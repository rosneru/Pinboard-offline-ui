<Window x:Class="UI.Desktop.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Interactivity="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:Views="clr-namespace:UI.Desktop.WPF.Views"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:md="https://github.com/fantasticfiasco/mvvm-dialogs"
        xmlns:viewmodel="clr-namespace:Logic.UI.ViewModels;assembly=Logic.UI"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Pinboard.in offline viewer"
        Width="1066"
        Height="600"
        d:DataContext="{d:DesignInstance Type=viewmodel:MainViewModel}"
        md:DialogServiceViews.IsRegistered="True"
        WindowState="Maximized"
        mc:Ignorable="d">
  <Window.InputBindings>
    <KeyBinding Key="P"
                Command="{Binding OpenSettingsCommand}"
                Modifiers="Alt" />
    <KeyBinding Key="U"
                Command="{Binding OpenUpdateCommand}"
                Modifiers="Alt" />
  </Window.InputBindings>
  <Interactivity:Interaction.Triggers>
    <Interactivity:EventTrigger EventName="Loaded">
      <Interactivity:InvokeCommandAction Command="{Binding LoadedCommand}" />
    </Interactivity:EventTrigger>
    <Interactivity:EventTrigger EventName="Closing">
      <Interactivity:InvokeCommandAction Command="{Binding ExitCommand}" PassEventArgsToCommand="True" />
    </Interactivity:EventTrigger>
  </Interactivity:Interaction.Triggers>
  <Grid Margin="5">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" MinWidth="100" />
      <ColumnDefinition Width="5" />
      <ColumnDefinition Width="3*" />
    </Grid.ColumnDefinitions>

    <Menu Grid.ColumnSpan="3">
      <MenuItem Header="_Project">
        <MenuItem Header="_About.." />
        <Separator />
        <MenuItem Command="{Binding OpenSettingsCommand}"
                  Header="_Settings.."
                  InputGestureText="Alt+P" />
        <MenuItem Command="{Binding OpenUpdateCommand}"
                  Header="_Update.."
                  InputGestureText="Alt+U" />
        <Separator />
        <MenuItem Command="{Binding ExitCommand}"
                  Header="_Exit"
                  InputGestureText="Alt+F4" />
      </MenuItem>
    </Menu>

    <Views:BookmarksListView Grid.Column="0"
                             Grid.Row="1"
                             Grid.RowSpan="2"
                             DataContext="{Binding BookmarksListViewModel}" />


    <GridSplitter Grid.Column="1"
                  Grid.Row="1"
                  Grid.RowSpan="2"
                  Width="5"
                  HorizontalAlignment="Stretch" />


    <StackPanel Grid.Column="2"
                Grid.Row="1"
                Orientation="Horizontal">
      <Label>Filter Panel</Label>
      <Button Content="Filter 1" />
      <Button Content="Filter 2" />
    </StackPanel>

    <wv2:WebView2 x:Name="wv"
                  Grid.Column="2"
                  Grid.Row="2" />

    <Views:StatusBarView Grid.ColumnSpan="3" Grid.Row="3" />

  </Grid>
</Window>
