<UserControl x:Class="UI.Desktop.WPF.Views.BookmarkList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:UI.Desktop.WPF.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UI.Desktop.WPF.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
  <UserControl.Resources>
    <converters:BackgroundColorConverter x:Key="BackgroundColorConverter" />
  </UserControl.Resources>
  <ListView Grid.ColumnSpan="2"
            Grid.Row="0"
            FocusVisualStyle="{x:Null}"
            ItemsSource="{Binding BookmarkService.FilteredBookmarks}"
            PreviewMouseWheel="BookmarksListView_PreviewMouseWheel"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
            SelectedItem="{Binding SelectedBookmark,
                                   ValidatesOnDataErrors=False}"
            SelectionMode="Single">
    <ListView.ItemContainerStyle>
      <Style TargetType="{x:Type ListViewItem}">
        <Setter Property="BorderBrush" Value="LightGray" />
        <Setter Property="BorderThickness" Value="1 0 0 1" />
      </Style>
    </ListView.ItemContainerStyle>
    <ListView.ItemTemplate>
      <DataTemplate>
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>
          <TextBlock Grid.Row="0" Text="{Binding Time}" FontStyle="Italic" Foreground="SteelBlue"/>
          <TextBlock Grid.Row="1"
                     FontWeight="Bold"
                     Text="{Binding Description}"
                     TextWrapping="Wrap" />
          <ListView Grid.Row="2"
                    Background="Transparent"
                    BorderThickness="0"
                    Focusable="False"
                    ItemsSource="{Binding TagsArray}"
                    PreviewMouseDown="TagsListView_PreviewMouseDown"
                    PreviewMouseWheel="TagsListView_PreviewMouseWheel"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                    SelectionChanged="TagsListView_SelectionChanged">
            <ListView.ItemsPanel>
              <ItemsPanelTemplate>
                <WrapPanel Orientation="Horizontal" />
              </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
              <DataTemplate>
                <Border Margin="0"
                        BorderThickness="1"
                        CornerRadius="5">
                  <Border.Background>
                    <MultiBinding Converter="{StaticResource BackgroundColorConverter}">
                      <Binding Path="." />
                      <!--  Das aktuelle Item  -->
                      <Binding Path="DataContext.BookmarkService.FilteredTags" RelativeSource="{RelativeSource AncestorType={x:Type UserControl}}" />
                      <!--  Die zweite Liste  -->
                    </MultiBinding>
                  </Border.Background>
                  <TextBlock Padding="0"
                             HorizontalAlignment="Center"
                             VerticalAlignment="Center"
                             Foreground="White"
                             Text="{Binding}" />
                </Border>
              </DataTemplate>
            </ListView.ItemTemplate>
          </ListView>
        </Grid>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>
</UserControl>
